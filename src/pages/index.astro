---
import Layout from "../layouts/Layout.astro";
import Bar from "../components/Bar.svelte";
import Map from "../components/Map.svelte";
import Bank from "../components/Bank.astro";
import Bubble from "../components/Bubble.svelte";
import EmployMap from "../components/EmployMap.svelte";
import Greeting from "../components/Greeting.astro";
import DynamicChart from "../components/DynamicChart.svelte";
const greeting = "Hi, welcome to visualization";
---

<Layout title={greeting}>
  <main>
    <section>
      <div><Greeting /></div>
    </section>

    <div class="nextstep">
      <section>
        <div><Bank /></div>
      </section>
      <section>
        <h1>ใกล้กัน?</h1>
      </section>
      <section>
        <div id="ldr"><Map client:only="svelte" /></div>
      </section>

      <section>
        <div>
          <h2>
            ตั้งแต่ปี 2550 ที่ผ่านมา อัตราส่วนสินเชื่อต่อเงินฝาก เพิ่มมาแค่
            14.03%
          </h2>
        </div>
        <div>
          <h2>แต่อัตราส่วนเงินกู้ต่อเงินฝากของกรุงเทพ เพิ่มมาเป็น 24.78%</h2>
        </div>
        <div><Bar client:only="svelte" /></div>
        <div>
          <h2 id="sme">
            พอต่างจังหวัดไม่ค่อยเข้าถึงแหล่งเงินทุน<br />ก็ทำให้ SME
            ในต่างจังหวัดน้อยลง<br />ไม่เกิดการจ้างงาน
          </h2>
        </div>
        <img src="/mindblow.gif" />
        <div><h2 id="meme">ไม่จ้างก็เข้ากรุงเทพสิครับพี่น้อง</h2></div>
      </section>
      <section>
        <div><EmployMap client:only="svelte" /></div>
      </section>
      <section>
        <div><Bubble client:visible /></div>
      </section>
      <section>
        <div>
          <h3>
            แทนที่เงินฝากของคนต่างจังหวัด จะถูกนำมาหมุนเวียนในจังหวัดตัวเอง
            ทำไมเงินของคนจังหวัด สุโขทัย พิจิตร ระนอง หรือ
            จังหวัดอื่นๆต้องถูกเอาไปให้คนกรุงเทพกู้? สรุปแล้ว เรื่องเงินฝาก
            ก็เหมือนเรื่องภาษีหรืออีกหลายๆเรื่อง ที่ทรัพยากรต่างจังหวัด
            ต้องถูกโยกไปให้ส่วนกลางอย่างกรุงเทพ
            ทำให้เกิดความเหลื่อมล้ำทางการพัฒนา
          </h3>
        </div>
        <div><DynamicChart client:only="svelte" /></div>
      </section>
    </div>
  </main>
</Layout>

<style>
  section:nth-child(1) {
    background: linear-gradient(to bottom, blue 52%, white 48%);
    width: 100vw;
    color: white;
  }
  section:nth-child(2) {
    background-color: blue;
    height: 100vh;
  }
  h1:first-child {
    color: white;
    font-size: 8em;
  }
  #meme {
    margin-bottom: 10%;
    margin-top: 5%;
  }
  h3 {
    text-align: center;
    width: 80vw;
    margin-bottom: 2%;
  }
  .nextstep {
    display: none;
  }
  #sme {
    text-align: center;
    margin-bottom: 4vh;
    margin-top: 4vh;
  }
</style>
<script is:inline src="https://code.jquery.com/jquery-3.7.0.min.js"></script>
<!-- <style>
  main {
    margin: auto;
    padding: 1rem;
    width: 800px;
    max-width: calc(100% - 2rem);
    color: white;
    font-size: 20px;
    line-height: 1.6;
  }
  .astro-a {
    position: absolute;
    top: -32px;
    left: 50%;
    transform: translatex(-50%);
    width: 220px;
    height: auto;
    z-index: -1;
  }
  h1 {
    font-size: 4rem;
    font-weight: 700;
    line-height: 1;
    text-align: center;
    margin-bottom: 1em;
  }
  .text-gradient {
    background-image: var(--accent-gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-size: 400%;
    background-position: 0%;
  }
  .instructions {
    margin-bottom: 2rem;
    border: 1px solid rgba(var(--accent-light), 25%);
    background: linear-gradient(
      rgba(var(--accent-dark), 66%),
      rgba(var(--accent-dark), 33%)
    );
    padding: 1.5rem;
    border-radius: 8px;
  }
  .instructions code {
    font-size: 0.8em;
    font-weight: bold;
    background: rgba(var(--accent-light), 12%);
    color: rgb(var(--accent-light));
    border-radius: 4px;
    padding: 0.3em 0.4em;
  }
  .instructions strong {
    color: rgb(var(--accent-light));
  }
  .link-card-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(24ch, 1fr));
    gap: 2rem;
    padding: 0;
  }
</style>-->
